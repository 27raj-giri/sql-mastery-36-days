--1
SELECT COUNT(*)
FROM orders;

--2
SELECT SUM(amount)
FROM orders;

--3
SELECT AVG(amount)
FROM orders;

--4
SELECT MIN(amount)
FROM orders;

--5
SELECT MAX(quantity)
FROM orders;
